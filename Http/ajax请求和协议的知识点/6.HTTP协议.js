/**
*  HTTP: 1.是超文本传输协议，是服务器向浏览器传输超文本到浏览器的传输协议，
*          它可以使浏览器更加高效。
*        2.http是由请求和响应构成，是一个标准的客户端服务器模型，http协议永远是由客户端发起的，
*           服务器返回响应。
*/
  

// HTTP 的工作过程

/**
 * 
 *   1.进行地址解析
 *   2.封装HTTP请求数据包
 *   3.封装成TCP包，建立TCP连接
 *   4.客户端发送请求命令
 *   5.服务器响应
 *   6.服务器关闭TCP连接
 * 
*/    


// HTTPS
/**
 *   1.HTTPS是HTTP的安全版，在HTTP下加入了SSl层，所用的端口号是443.
 *   2.SSL这种安全传输协议是通过证书认证来确保客户端和网站服务器之间的
 *     通信数据是加密安全的
*/